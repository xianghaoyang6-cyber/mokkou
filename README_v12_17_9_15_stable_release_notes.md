# 我の木工 PWA v12.17.9.15 計算頁 7/8 高度壓縮版

## 本版目的

壓縮計算頁第 7、8 區塊高度，減少手機畫面垂直占用。

## 本版完成

1. 將「7. 目前檢查提醒」改為「7. 檢查」。
2. 第 7 區改成折疊卡，不再用固定展開的大卡片。
3. 第 7 區內容改成短狀態列、4 格摘要與三顆短操作鍵。
4. 將「8. 計算結果 / 板件清單」改為「8. 結果」。
5. 第 8 區改成施工用短摘要與緊湊板件清單。
6. 保留尺寸計算、SVG 幾何、榫卯與板件輸出邏輯。
7. 保留 v12.17.9.14 的底板與輸出高度壓縮。
8. 保留 v12.17.9.13 的 state 錯誤修正。
9. 保留 v12.17.9.6 的 SVG 樣式外洩修正。

## 建議驗收

1. 首頁是否顯示 v12.17.9.15。
2. 計算頁是否正常進入。
3. 展開「7. 檢查」是否比舊版更矮。
4. 儲存、套圖紙、重設三顆按鈕是否正常。
5. 展開「8. 結果」是否改成短摘要與兩欄板件清單。
6. 圖紙頁全螢幕是否仍沒有 CSS 程式碼。
